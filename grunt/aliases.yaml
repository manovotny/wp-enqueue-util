default:
  - build

init:
  - config
  - shell:install
  - update
  - build

config:
  - replace
  - rename

build:
  - clean:js
  - concat
  - sass

generate:
  - makepot
  - concat
  - uglify

update:
  - clean:lib
  - shell:update
  - copy
  - curl

lint:
  - jshint
  - jslint
  - phplint

test:
  - phpunit

release:
  - lint
  - test
  - build
  - generate
  - replace:project_version