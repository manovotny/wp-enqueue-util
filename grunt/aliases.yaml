default:
  - build

init:
  - config
  - shell:install
  - update
  - build

config:
  - replace
  - rename

build:
  - lint
  - test

generate:
  - makepot

update:
  - clean:lib
  - shell:update
  - copy

lint:
  - jslint
  - phplint

test:
  - phpunit

release:
  - lint
  - test
  - build
  - replace:project_version
  - generate
